//the logical or operator returns true if either of the operands is 
//true if otherwise it returns false.

const a = 3;
const b = -2;

console.log(a > 0 || b > 0);
// expected output: true

//finish the topics later!!!!!!
